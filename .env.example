# Web Push certificate (public half) — Firebase console → Project settings →
# Cloud Messaging → Web configuration → "Web Push certificates" → Generate key pair.
# Copy this file to .env and paste the key. It ships in the client bundle by
# design (it's the public half of the pair), so it is not a secret.
VITE_FCM_VAPID_KEY=
